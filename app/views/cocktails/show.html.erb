<div class="container">
  <h1><%= @cocktail.name %></h1>
  <%= link_to "Add a dose", new_cocktail_dose_path(@cocktail) %>
  <h2>Liste d'ingrédients</h2>
    <% @cocktail.doses.each do |dose| %>
    <p><%= dose.ingredient.name %> - <%= dose.description %> - <%= link_to "Delete", dose_path(dose), method: :delete, class: "btn btn-primary" %></p>
    <% end %>

    <%= link_to "Back to cocktails", cocktails_path %>
</div>
